# Copy to .env and fill in the real values. Never commit .env.

# Supabase (safe to expose — these ship in the client bundle, RLS protects the data)
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Brevo transactional email (server-side only, used by api/send-email.ts)
BREVO_API_KEY=""

# CMS login used for local admin testing — keep secret
CMS_ACCOUNT=""
CMS_ACCOUNT_PASSWORD=""
