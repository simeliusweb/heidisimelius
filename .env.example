# .env is COMMITTED and holds only public VITE_* values (see the note in that file).
# Copy this to .env.local for anything secret — .env.local is gitignored.

# CMS login, used for local admin testing
CMS_ACCOUNT=""
CMS_ACCOUNT_PASSWORD=""

# Brevo transactional email. Set in the Vercel project env for production;
# only needed here if you run the serverless functions locally.
BREVO_API_KEY=""
